<Window x:Class="SensorimonitorReactionSimulatorV2._0.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:SensorimonitorReactionSimulatorV2._0.MVVM.ViewModels"
        mc:Ignorable="d"
        Title="Тренажер развивающий сенсомоторную реакцию" 
        Height="720" 
        Width="1280">

    <Window.DataContext>
        <viewModels:MainWindowViewModel/>
    </Window.DataContext>

    <ContentControl x:Name="MainWindowContent"
                    Content="{Binding CurrentMainWindowContent}"/>

</Window>
